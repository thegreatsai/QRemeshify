services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ndp
      POSTGRES_PASSWORD: ndp
      POSTGRES_DB: ndp
    ports:
      - "5432:5432"
    volumes:
      - ndp_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ndp"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build: ./backend
    environment:
      NDP_DATABASE_URL: postgresql+psycopg2://ndp:ndp@db:5432/ndp
      NDP_CORS_ORIGINS: '["http://localhost:5173"]'
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
    command: >
      sh -c "alembic upgrade head &&
             uvicorn app.main:app --host 0.0.0.0 --port 8000"

volumes:
  ndp_pgdata:
